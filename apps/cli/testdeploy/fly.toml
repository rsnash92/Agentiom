# Fly.io configuration for testdeploy
# https://fly.io/docs/reference/configuration/

app = "agentiom-agents"
primary_region = "iad"

[build]
# Uses Dockerfile in current directory

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[mounts]
  source = "vol_testdeploy"
  destination = "/data"

[env]
  AGENT_NAME = "testdeploy"
  LOG_LEVEL = "INFO"
